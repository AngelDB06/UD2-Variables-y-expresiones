package Relacion5;

public class ej3 {
    public static void main(String args[]) {
    // a)
    double resultadoa = (3.0 / 2.0) + (4.0 / 3.0);

    // b)
    double resultadob = (1.0 / x - 5.0) - (3.0 * x * y / 4.0);

    // c)
    double resultadoc = (1.0 / 2.0) + 7.0;

    // d)
    double resultadod = 7.0 + (1.0 / 2.0);

    // e)
    double resultadoe = ((a * a) / b - c) + (d - e) / (f - ((g * h) / j));

    // f)
    double resultadof = (m / n) + p;

    // g)
    double resultadog = m + (n / (p - q));

    // h)
    double resultadoh = (a * a) / (b * b) + (c * c) / (d * d);

    // i)
    double resultadoi = (m + n / p) / (q - r / s);

    // j)
    double resultadoj = (3.0 * a + b) / ((c - (d + 5.0 / e)) / (f + g / (2.0 * h)));

    // k)
    double resultadok = ((a * a) + (2.0 * a * b) + (b * b)) / (((1.0 * x) / 2.0) + 2.0);
        
    }
}
